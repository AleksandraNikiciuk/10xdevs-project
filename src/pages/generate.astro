---
import Layout from "../layouts/Layout.astro";
import { GenerateView } from "../components/generate/GenerateView";

const { user } = Astro.locals;
const isUserLoggedIn = !!user;
---

<Layout>
  <GenerateView client:load isUserLoggedIn={isUserLoggedIn} />
</Layout>
